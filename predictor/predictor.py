# Input: np array of asset montly return, standard deviation; np array of factor data
# Output: np array of predicted asset monthly expected return, standard deviation and covariance
# Use Garch and Bayesian Network
def aiPredictor():
    pass

# Input: np array of asset montly return, standard deviation; np array of factor data
# Output: np array of predicted asset monthly expected return, standard deviation and covariance
# Use Bootstrap to return random prediction
def randomPredictor():
    pass